# 整改方案 · TD 记忆链路契约与工程风险

> 实测时间：2026-09-21 16:20–16:35（NAS 在线）
> 实测对象：面板 `http://muhuihao.top:8125`、Proxy `http://muhuihao.top:8096`
> 证据来源：① 面板前端 `assets/main-CdwHEhgx.js` 反解出的真实调用契约；② 用真实 user_key 逐端点 curl 实测
> 结论一句话：**MCP 的 10 个工具里只有 1 个参数是对的，其余 9 个全部错；且项目里硬编码的资产 ID 已全部失效。**

---

## 一、实测发现的两个"静默失效"

### 1.1 NAS 已重新部署，旧资产 ID 全部作废

| 项 | 项目里写死的旧值 | 实测当前值 | 状态 |
|---|---|---|---|
| user_id | `usr-zcdxzdq0o1` | `usr-zm2fcsoem0`（huihui / system_admin） | ❌ 已变 |
| team_id | `team-zcv52tgzwg` | `team-zn0elw0289`（HUI） | ❌ 已变 |
| agent_id | `agt-zc6vu8z5ks`（通用开发助手） | `agt-zoav7zxdz0`（码聊助手） | ❌ 已变 |
| task_id | `task-zdlxl0mp4h`（ZCode 日常开发） | 无（`task/list` 返回 0 条） | ❌ 不存在 |
| user_key | `sk-mem-Yjhssg20010303` | 同一把，仍有效 | ✅ 可用 |

面板 `created_at` 为 `2026-09-21T07:00:05Z`（= 北京时间今天 15:00），说明这套栈是**今天下午重新起的新卷**，旧数据不在这个卷里。

**危害**：这些 ID 作为 `team_id` 传进去，面板**不报错**，一律返回 `{"items":[],"total":0}`。模型看到的是"这个团队没有记忆"，而不是"配置错了"——最坏的一种失败方式。

硬编码位置（3 处，都要清）：
- `core/tdai-core.js` L15–L17（DEFAULT_TEAM / DEFAULT_AGENT / DEFAULT_TASK）
- `pet/src/main.js` L31–L33（defaultCfg.panel 三件套）
- 本机 `~/.zcode/tdai-mcp.json`（实际生效的那份）

### 1.2 直连 Proxy 拿不到任何注入

```
POST http://muhuihao.top:8096/dsh/default/v1/chat/completions
Authorization: Bearer sk-mem-...      （无 x-team-id / x-agent-id / x-task-id / x-conversation-id）
→ prompt_tokens = 84
```
和你们文档里记录的"直连 84 / 走网关 3600"完全吻合。即：**ZCode 直连 8096 只是纯透传，记忆注入为 0**。这不是 bug，是 header 四件套缺失导致的必然结果。

---

## 二、MCP 工具契约错误清单（P0，全部必改）

### 2.1 真实契约（反解 + 实测双确认）

面板 API 统一：`POST http://<panel>/api/v1/<面>/<action>`，头 `X-Tdai-Service-Id` + `X-Tdai-User-Key`，响应 `{code, message, request_id, data}`，`code !== 0` 即失败。

| 面 | action | **真实参数** |
|---|---|---|
| meta | `auth/verify` | `{user_key}` ← **必须放 body** |
| meta | `team/list` | `{user_key}` |
| meta | `agent/list` | `{team_id, user_key}` |
| meta | `task/list` | `{team_id, user_key}` |
| chat-memory | `my-agents` | `{team_id}` → 返回 `items[].id` = **block_id** |
| chat-memory | `team-assets` | `{team_id}` |
| chat-memory | `search` | `{block_id, layer, query, limit, type?}` |
| chat-memory | `layer` | `{block_id, layer, limit, offset, path?, before_ts?, time_start?, time_end?}` |
| chat-memory | `create` | `{team_id, title, scope, description}` |
| chat-memory | `layer-update` | `{block_id, layer, id?, content, summary?}` |
| chat-memory | `import` | `{team_id, agent_id, messages, session_id}` |
| skill | `list` | `{user_id, team_id, agent_id, filters{}, pagination{limit,offset}}` |
| skill | `get` | `{user_id, team_id, skill_id, version, include_content, include_manifest}` |
| skill | `versions` / `extract` / `export` / `create` / `update` / `delete` | 见前端 `ka(...)` |
| skill | `files/read` `files/write` `files/remove` | 资源文件读写 |
| knowledge | `health` | 当前返回 **404**（该端点不存在，别再调） |
| knowledge | `wiki/list` | `{team_id}` → 返回 `wiki_id` |
| knowledge | `wiki/search` | `{wiki_id, query, limit}` |
| knowledge | `wiki/page/ls` | `{wiki_id}` |
| knowledge | `wiki/page/read` | `{wiki_id, refs:[path]}` |
| knowledge | `wiki/raw/ls` `wiki/raw/read` | `{wiki_id}` / `{wiki_id, filenames}` |
| knowledge | `wiki/graph` | `{wiki_id}` |
| knowledge | `code-graph/list` | `{team_id}` → 返回 `code_graph_id` |
| knowledge | `code-graph/search` | `{code_graph_id, query, kind?, limit}` |
| knowledge | `code-graph/explore` | `{code_graph_id, query}` |

### 2.2 逐条比对（`core/tdai-core.js`）

| 工具 | 当前实现 | 真实契约 | 实测结果 | 判定 |
|---|---|---|---|---|
| `health()` | `GET /meta/auth/verify` 无 body | `POST` + body `{user_key}` | `400 user_key: Invalid input: expected string` | ❌ |
| `memorySearch()` | `{query, top_k, team_id, agent_id}` | `{block_id, layer, query, limit}` | `400 MISSING_BLOCK_ID` | ❌ |
| `memoryLayers()` | `{team_id, agent_id}` | `{block_id, layer, limit, offset}` | `400 MISSING_BLOCK_ID` | ❌ |
| `teamAssets()` | `{team_id}` | `{team_id}` | `code:0 ok` | ✅ **唯一正确** |
| `skillList()` | `{team_id}` | `{user_id, team_id, filters, pagination}` | `code:0` 但 `total:0`（静默空） | ❌ |
| `skillGet()` | `{skill_id}` 或 `{name, team_id}` | `{user_id, team_id, skill_id, ...}`，**无 name 参数** | 参数不存在 | ❌ |
| `wikiSearch()` | `{query, top_k, team_id}` | `{wiki_id, query, limit}` | 缺 wiki_id | ❌ |
| `wikiRead()` | `{page_id}` | `{wiki_id, refs:[path]}` | 参数不存在 | ❌ |
| `codegraphSearch()` | `{query, top_k, team_id}` | `{code_graph_id, query, kind?, limit}` | 缺 code_graph_id | ❌ |
| `codegraphExplore()` | `{node_id, depth, team_id}` | `{code_graph_id, query}` | 参数不存在 | ❌ |

**漏掉的 4 个入口工具**（没有它们拿不到下游 ID，检索链根本走不通）：

| 应新增 | 作用 |
|---|---|
| `tdai.my_agents` | team_id → **block_id**（记忆检索的唯一入口） |
| `tdai.wiki_list` | team_id → wiki_id |
| `tdai.codegraph_list` | team_id → code_graph_id |
| `tdai.auth_verify` | user_key → user_id（skill/list 需要 user_id） |

### 2.3 最关键的认知缺口：`block_id`

记忆**不是挂在 team/agent 上，而是挂在"记忆块(block)"上**：

```
block_id = "chat_memory-<team_id>-<agent_id>"
实测样例：chat_memory-team-zn0elw0289-agt-zoav7zxdz0
```

正确调用链（缺一环就 `MISSING_BLOCK_ID`）：

```
auth/verify {user_key}                     → user_id
  └─ my-agents {team_id}                   → items[].id = block_id   ← 当前项目完全没有这步
       ├─ chat-memory/search {block_id, layer, query, limit}   → 记忆片段
       └─ chat-memory/layer  {block_id, layer, limit, offset}  → 分层明细
```

实测 `layer` 用 L0 已能取回真实消息（`msg-f5d9b15c...`，assistant @ zcode:bc346924…），而 L1/L2/L3 均为 0 条——**说明 L0 已落库，但 L1/L2/L3 抽取还没跑**。这块要看 `promptMode` 与抽取阈值。

---

## 三、MCP 协议层缺陷（P1）

| # | 问题 | 位置 | 修法 |
|---|---|---|---|
| 1 | 工具执行失败未设 `isError: true` | `mcp/tdai-mcp.js` `mcpText()` | 失败时返回 `{content:[...], isError:true}`，否则模型会把 `{ok:false}` 当正常结果读 |
| 2 | `initialize` 直接回显客户端 protocolVersion，未协商 | `handle()` case `initialize` | 与自身支持列表（`2024-11-05`）取交集，不认识就回自己最高版本 |
| 3 | 客户端断开后进程残留 | `serveMCP()` | 加 `rl.on('close', () => process.exit(0))` |
| 4 | 无重试 / 退避 | `core.requestRaw` | 网络类错误重试 1 次；5xx 退避 |
| 5 | `clip()` 对 JSON 字符串硬切 | `core.clip` | 结构化截断（数组截前 N 项、单字段截长），避免半截 JSON |
| 6 | 未声明 `resources` / `prompts` 能力 | `initialize` | 可选；客户端探测时会返 `-32601`，属合规但不够干净 |
| 7 | stdout 协议污染风险 | `tdai-mcp.js` | MCP 模式下把 `console.log` 全部改 `console.error`（当前仅 stderr，基本安全，加注释护栏） |

---

## 四、工程与分发风险（P0 发布级）

### 4.1 硬编码私有资产 → 打包发出去等于把 NAS 送人

`core/tdai-core.js` L14–L17：

```js
const DEFAULT_PANEL = 'http://muhuihao.top:8125';   // ← 你的公网面板
const DEFAULT_TEAM  = 'team-zcv52tgzwg';            // ← 你的私有资产
const DEFAULT_AGENT = 'agt-zc6vu8z5ks';
const DEFAULT_TASK  = 'task-zdlxl0mp4h';
```

`pet/package.json` 的 `build.publish` 指向 GitHub `HUIdada1/tencentdb-memory-mcp`，`dist/win-unpacked` 已构建。**任何人下载这个 exe，默认就会连你的 NAS**（key 虽然为空，但地址和资产 ID 已经外泄）。

修法：
1. 全部改为空串占位；`panelUrl` 缺省时走首次运行引导页。
2. 加启动自检：`panelUrl` 为空或含 `muhuihao.top` 字样且非本机配置 → 弹配置向导。
3. 检查 `dist/`、`release/` 是否被 git 跟踪（`.gitignore` 已含 `dist/`，但要确认历史提交里没有明文 key）。**建议对 `sk-mem-Yjhssg20010303` 做一次 key 轮换**，因为它已出现在 md 文档与配置里。

### 4.2 配置双份、密钥两处

| 消费者 | 配置文件 | 内容 |
|---|---|---|
| MCP server | `~/.zcode/tdai-mcp.json` | panelUrl / userKey / teamId / agentId / taskId |
| Electron pet | `~/.tdai-pet/config.json` | 同一组字段，另一份 |

`pet/src/main.js` L104–113 有一段"userKey 为空就从 zcode 迁移"的补丁——说明作者已经踩到这个坑，但只是打补丁，没统一。

修法：单一配置源 `~/.tdai-memory/config.json`，MCP 与 pet 都读它；pet 负责写；旧路径做一次性迁移。

### 4.3 Node 路径写死

`E:\nvm\v22.22.0\node.exe` 硬编码在 3 处：
- `.mcp.json`
- `mcp/register-zcode.cjs` L18
- `~/.zcode/cli/config.json`

别人机器上必然不存在。修法：改 `"command": "node"`（依赖 PATH），或安装脚本探测 `process.execPath` 后回填。

### 4.4 `skills/tdai-memory/` 是空目录

方案 B 第 5.4 节承诺的"ZCode skill 兜底（无 MCP 时走 CLI）"**没有落地**。当前该目录 0 个文件。

修法：补 `SKILL.md`，内容为触发条件 + `node mcp/tdai-mcp.js run <tool> --json '{...}'` 用法 + 工具清单。

### 4.5 密钥明文落盘，无权限收紧

`~/.zcode/tdai-mcp.json` 是普通权限明文。对比官方 dsh 的做法（`chmod 700 ~/.dsh` + `chmod 600 .credentials.yaml`）。

修法：Windows 上至少加 ACL 收紧；跨平台用 `fs.chmodSync(p, 0o600)`（POSIX 有效）。

---

## 五、与官方 Proxy 路线的能力差距（架构层）

| 能力 | 官方 Proxy 路线 | 本项目 MCP 路线 | 差距性质 |
|---|---|---|---|
| 自动注入 L2/L3 + skill + knowledge | ✅ 每轮无感注入 | ❌ 靠模型主动调用 | **最大差距** |
| 自动归档 L0 | ✅ 全自动 | ❌ 项目内已无（原 `mem-uploader.cjs` 在 3.4 节被清理） | **第二差距** |
| 首会话 team/agent/task 选择 | ✅ 交互式 form | ❌ 配置文件写死 | 多环境不便 |
| 会话隔离 | ✅ session-id 维度 | ⚠️ MCP 无会话概念，检索无上下文 | 检索精度下降 |
| 写入能力 | ✅ 全链路 | ❌ 全只读（设计选择） | 可接受 |
| 客户端覆盖 | 7 家已适配，需逐家改代码 | 任何支持 MCP 的客户端即插即用 | **MCP 反而更广** |

**结论：MCP 路线不是 Proxy 的替代品，是互补品。** 它精确解决"客户端发不了自定义 header / 响应不了 form"的场景——ZCode 正是这一类。两者可并存：ZCode 走 MCP，dsh / CC / CB / Codex / WorkBuddy 走 Proxy。

---

## 六、ZCode 与其它 IDE 的接入方式（三条路）

### 路 1 · MCP（推荐，本项目当前路线）

| 客户端 | 接入位置 | 状态 |
|---|---|---|
| ZCode CLI | `~/.zcode/cli/config.json` → `mcp.servers.tdai` | ✅ 已注册（node 路径需修） |
| ZCode 桌面端 | 设置 → MCP 服务器 → 新建，或"从外部 Agent 导入" `.mcp.json` | 需手动点一次 |
| Claude Code / Cursor / 其它 | 项目根 `.mcp.json` 标准格式 | ✅ 已就位 |

**关键前提**：MCP 是"拉"模型，需要引导。建议在 `skills/tdai-memory/SKILL.md` 里写死触发规则，例如"用户问『我们之前关于 X 是怎么做的』『查一下团队记忆』时，先调 `tdai.my_agents` 拿 block_id，再调 `tdai.memory_search`"。

### 路 2 · 本地 header 注入网关（若需要"自动注入"则必须重建）

```
ZCode（任意供应商渠道）
  → http://127.0.0.1:8099/dsh/default/v1
  → 网关补齐 x-team-id / x-agent-id / x-task-id / x-conversation-id
  → http://muhuihao.top:8096
```

- 实测已确认：不走网关 = `prompt_tokens 84`（零注入）；走网关 = 3600。**ZCode 发不了自定义 header 是硬约束，没有捷径。**
- 优点：自动注入 + 自动归档，体验对齐 Claude Code。
- 缺点：需常驻进程（可用 Electron pet 顺带托管，不必再写 VBS）。

### 路 3 · Skill + CLI 兜底（不支持 MCP 的客户端）

`node mcp/tdai-mcp.js run tdai.memory_search --json '{"query":"..."}'`
—— 但需先补 `skills/tdai-memory/SKILL.md`（见 4.4）。

### 附：支持 header 的客户端直接走官方 Proxy，无需 MCP

| 客户端 | 配置 |
|---|---|
| Claude Code | `ANTHROPIC_BASE_URL=http://<host>:8096/claude-code/default` + `ANTHROPIC_AUTH_TOKEN=<user_key>` |
| dsh | `~/.dsh/settings.yaml` → `baseURL: http://<host>:8096/dsh/default`（**不带 `/v1`**） |
| CodeBuddy | `~/.codebuddy/models.json` |
| Codex | `~/.codex/config.toml`（首轮需切 Plan 模式） |
| WorkBuddy | `~/.workbuddy/models.json` |

---

## 七、整改清单（按优先级）

### P0 · 不做则功能不可用

| # | 事项 | 文件 | 工作量 |
|---|---|---|---|
| 1 | 按 §2.1 真实契约重写全部工具参数 | `core/tdai-core.js` | 2h |
| 2 | 引入 `block_id` 链路（my-agents → block_id → search/layer） | `core/tdai-core.js` + `mcp/tdai-mcp.js` | 1h |
| 3 | 新增 4 个入口工具（my_agents / wiki_list / codegraph_list / auth_verify） | `mcp/tdai-mcp.js` | 1h |
| 4 | 清空硬编码私有资产 ID 与公网地址 + 首次运行引导 | `core/tdai-core.js`、`pet/src/main.js` | 1.5h |
| 5 | 更新本机 `~/.zcode/tdai-mcp.json` 为新 team/agent（或删掉 taskId） | 本机配置 | 5min |
| 6 | 补 `skills/tdai-memory/SKILL.md` | `skills/tdai-memory/` | 1h |

### P1 · 健壮性与合规

| # | 事项 | 文件 |
|---|---|---|
| 7 | `tools/call` 失败加 `isError: true` | `mcp/tdai-mcp.js` |
| 8 | `initialize` 协议版本协商 + `rl.on('close')` 退出 | `mcp/tdai-mcp.js` |
| 9 | 结构化截断替代字符串截断 | `core/tdai-core.js` |
| 10 | 统一配置源为 `~/.tdai-memory/config.json` + 旧路径一次性迁移 | `core/` + `pet/src/main.js` |
| 11 | Node 路径改 `node` 或安装时探测 | `.mcp.json`、`register-zcode.cjs` |
| 12 | 配置文件权限收紧（0o600） | `core/tdai-core.js` |
| 13 | 密钥轮换（`sk-mem-Yjhssg20010303` 已外泄到文档） | NAS 面板 |

### P2 · 体验增强

| # | 事项 |
|---|---|
| 14 | 控制台内做 team/agent/task 选择器，写回配置（替代手改 JSON） |
| 15 | 可选写工具 `tdai.memory_add` / `tdai.layer_update` |
| 16 | 用 Electron pet 托管本地网关（若要"自动注入"则走这条路，不必再写 VBS） |
| 17 | 会话上下文透传：MCP 无 session 概念，可用 `conversation_id` 参数近似 |

---

## 八、验收命令（改完直接跑）

```bash
# 0) 配置就位后，逐层验证
K="<你的 user_key>"; B="http://muhuihao.top:8125/api/v1"
H=(-H "Content-Type: application/json" -H "x-tdai-service-id: default" -H "x-tdai-user-key: $K")

# 1) 认证 → 拿 user_id
curl -s -X POST "$B/meta/auth/verify" "${H[@]}" -d "{\"user_key\":\"$K\"}"

# 2) 拿 team_id
curl -s -X POST "$B/meta/team/list" "${H[@]}" -d "{\"user_key\":\"$K\"}"

# 3) 拿 block_id（关键一步）
curl -s -X POST "$B/chat-memory/my-agents" "${H[@]}" -d '{"team_id":"<TEAM_ID>"}'

# 4) 用 block_id 检索（把 <BLOCK_ID> 换成上一步 items[0].id）
curl -s -X POST "$B/chat-memory/search" "${H[@]}" \
  -d '{"block_id":"<BLOCK_ID>","layer":"L1","query":"记忆","limit":5}'
curl -s -X POST "$B/chat-memory/layer" "${H[@]}" \
  -d '{"block_id":"<BLOCK_ID>","layer":"L0","limit":3,"offset":0}'

# 5) 技能目录（注意 user_id 必填）
curl -s -X POST "$B/skill/list" "${H[@]}" \
  -d '{"user_id":"<USER_ID>","team_id":"<TEAM_ID>","filters":{},"pagination":{"limit":100,"offset":0}}'

# 6) MCP 侧（本地）
node mcp/tdai-mcp.js list
node mcp/tdai-mcp.js run tdai.my_agents --json '{"team_id":"<TEAM_ID>"}'
node mcp/tdai-mcp.js run tdai.memory_search --json '{"query":"记忆","layer":"L1"}'
```

**通过标准**：第 3 步能拿到非空 `items[].id`；第 4 步 `code:0` 且不再出现 `MISSING_BLOCK_ID`；第 5 步 `total > 0`。

---

## 九、遗留待确认

1. `chat-memory/search` 的 `type` 参数取值域未知（前端是 `type?` 可选），需实测。
2. `knowledge/health` 返回 404——该端点不存在，`tdai.health` 需改探测方式（或直接去掉知识面健康检查）。
3. L1/L2/L3 全为 0 而 L0 有数据：需确认抽取是"对话轮次阈值触发"还是"需手动 force-archive"，必要时在面板侧触发一次。
4. `task/list` 为空，`x-task-id` 在 MCP 路线下不需要，但若重建网关方案则需先建 Task。
5. 面板 wiki 的 `wiki/search` 是两步（`wiki/list` → `wiki_id`），当前项目当成一步调用，属 §2.2 已列问题。
